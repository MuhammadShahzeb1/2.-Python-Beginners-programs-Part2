# it will append in a file like whatever we will write it will just add in exisiting file #if we do not use append operation than it wil just simply delete previous data update it with new one# it will append in a file like whatever we will write it will just add in exisiting file #if we do not use append operation than it wil just simply delete previous data update it with new one