#we will discuss appending a file
#f=open("append.py","a")   #it will append in a file like whatever we will write it will just add in exisiting file
                           #if we do not use append operation than it wil just simply delete previous data update it with new one
#now we can write our new program in this file

"""
f.write("#we will discuss appending a file \n")
f=open("append.py","a")

f.write("# it will append in a file like whatever we will write it will just add in exisiting file "
        "#if we do not use append operation than it wil just simply delete previous data update it with new one")

"""
